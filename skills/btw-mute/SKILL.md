---
name: btw-mute
description: Mute /btw-vc microphone (voice stays live). Use when the user says /btw-mute or wants to mute mic on Live voice.
---

Call MCP tool `btw_mute`. Confirm in one line: mic muted, session still live (or not live if runtime down).
