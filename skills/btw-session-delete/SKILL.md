---
name: btw-session-delete
description: Delete a /btw-vc voice session by name or id. Use for /btw-session-delete.
argument-hint: name-or-id
---

Take name or id. Call `btw_session_delete` with `{ "id_or_name": "..." }`.  
Confirm deleted and which session is now active.
