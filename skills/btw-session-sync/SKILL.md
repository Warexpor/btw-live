---
name: btw-session-sync
description: Refresh bound ChatGPT conversation metadata for active /btw-vc session. Use for /btw-session-sync.
---

# /btw-session-sync

Call `btw_session_sync` (requires prior `btw_session_bind`). Shows title, turn_count, preview, resume_chars. Does not start Live.

If unbound, point user to `/btw-session-bind`.
