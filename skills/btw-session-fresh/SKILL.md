---
name: btw-session-fresh
description: Clear ChatGPT conversation bind on active /btw-vc session. Use for /btw-session-fresh.
---

# /btw-session-fresh

Call `btw_session_fresh`. Keeps local pack/profile/voice; drops `conversation_id`. Next `/btw-vc` is an unbound Live mint.

Confirm session name and `resume: false`.
